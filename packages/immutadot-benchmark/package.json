{
  "name": "immutadot-benchmark",
  "version": "1.0.0-rc.7",
  "license": "MIT",
  "devDependencies": {
    "immer": "~0.6.0",
    "immutable": "~3.8.2",
    "immutadot": "~1.0.0-rc.7",
    "jest": "~21.2.1",
    "lerna": "~2.6.0"
  },
  "scripts": {
    "prebenchmark": "lerna run --scope immutadot build",
    "benchmark": "jest -i",
    "test": "echo No tests"
  }
}
